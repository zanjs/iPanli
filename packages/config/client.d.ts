declare const config: {
	APP: {
		APIPrefix: string;
	};
	cookie: string;
	token: string;
	Token: string;
	domain: string;
	wxappid: string;
	facebookappid: string;
};

export default config;
